{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "CMake: build",
			"command": "cmake --build ${workspaceFolder}/build",
			"group": {
				"kind": "build",
				"isDefault": true
			},
		}
	]
}
